# React + TypeScript Training Sandbox

Песочница для практики React и TypeScript на изолированных учебных задачах.

![Локальный вид песочницы с React-задачами](./docs/assets/sandbox-preview.png)

## Быстрый старт

1. Сделай fork или clone проекта.
2. Установи зависимости: `pnpm install`.
3. Запусти приложение: `pnpm dev`.
4. Выбери задачу в приложении.
5. Прочитай её `src/tasks/<task-id>/task.md`.
6. Решай задачу в `src/tasks/<task-id>/attempts/current/`.

## Где работать

```text
starter/
→ исходное состояние задачи, не редактируется

attempts/current/
→ текущая рабочая попытка

attempts/YYYY-MM-DD/
→ сохранённые предыдущие попытки
```

Приложение запускает рабочую копию из `attempts/current/` через `active.ts`. Чтобы начать задачу заново, восстанови `current` из `starter`.

Подробный workflow сохранения и повторного прохождения задач описан в [TASKS.md](./TASKS.md).

> Владелец исходного репозитория хранит личные решения в ветке `practice`, чтобы `main` оставался чистой базой. Другим пользователям создавать такую ветку необязательно.
